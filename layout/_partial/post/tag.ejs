<% if (post.tags && post.tags.length){ %>
	<div class="article-tag tagcloud">
        <i class="fa fa-tags"></i>
		<%- list_tags(post.tags, {
		    show_count: false,
		    class: 'article-tag'
		  }) %>
	</div>
<% } %>